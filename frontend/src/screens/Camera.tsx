import { ReactElement } from 'react';
import { Text } from 'react-native';

interface Camera {}

const Camera: React.FC = (): ReactElement => {
  return <Text>Camera</Text>;
};

export default Camera;
