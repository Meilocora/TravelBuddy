import { ReactElement } from 'react';
import { Text } from 'react-native';

interface JourneyDetailsProps {}

const JourneyDetails: React.FC<JourneyDetailsProps> = (): ReactElement => {
  return <Text>Journey Details</Text>;
};
